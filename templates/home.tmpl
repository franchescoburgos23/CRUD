{{ define "home"}}

{{ template "head"}}

<a name="" id="" class="btn btn-primary" href="/create" role="button">Add Employeer</a>

  <table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Email</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        <tr>

        {{range.}}
            <td>{{.Id}}</td>
            <td>{{.Name}}</td>
            <th>{{.Email}}</th>
            <th>Edit {{.Id}}| Delente {{.Id}}</th>
        </tr>
        <tr>

        {{end}}

           
        </tr>
    </tbody>
</table>

{{ template "foot" }}
    

{{end}}